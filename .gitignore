# Dependencies
/node_modules
/.pnp
.pnp.js
yarn.lock
pnpm-lock.yaml
# package-lock.json SÍ se versiona: el Dockerfile usa `npm ci` y lo necesita en el repo

# Testing
/coverage

# Next.js
/.next/
/out/
*.rsc

# Production
/build
/dist

# Misc
.DS_Store
*.pem
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env*.local
!.env.example

# Vercel
.vercel

# IDE
.vscode
.idea
*.swp
*.swo
*~
.sublime-project
.sublime-workspace

# OS
Thumbs.db
.AppleDouble
.LSOverride

# Claude Code
.claude/skills/**/node_modules
.claude/plugins
*.mcp.json
!example.mcp.json

# Supabase
supabase/.branches
supabase/.temp

# Secretos y datos personales (NUNCA subir)
.passwords
referencias/

# Asset de skill con API key embebida (no subir)
.claude/skills/video-visuals/assets/levy.png

# TypeScript build cache
*.tsbuildinfo

# Exports de conversación (contienen transcript con secretos leídos — NUNCA subir)
exports/

# Backup del env de Supabase (secretos — NUNCA subir)
.env.local.bak-supabase

# Artefactos de sesiones Playwright MCP (screenshots/logs de testing)
.playwright-mcp/
